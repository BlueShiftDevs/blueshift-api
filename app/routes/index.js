// const cards = require('./cards');

// // passes all routes out via JS index feature
// module.exports = function (app, db) {
//     cards(app, db);
//     // Other route groups go here in the future
// }