// module.exports = {
//     assignOnly: function(seed, second, fields){
//         const properties = second.entries.filter(e => fields.some(f => e.key === f));
//         const fill = Object.create({}, properties);
//         Object.assign(seed, )
//     }
// }